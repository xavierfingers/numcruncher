# NumCruncher - Calculating many square roots and GCD
### NumCruncher is a program to calculate GCD of numbers
# How it works
 ```mermaid
  flowchart LR
  A["User runs program"]->B["Uses algorithm: Euclidean GCD algorithm"]->C["Gets output"]
```   